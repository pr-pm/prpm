# CI Test Replit Rule

This is a test rule for PRPM integration testing.

## Guidelines

- Follow Replit configuration format
- Use markdown for project instructions
- Keep content focused

## Testing

This package verifies that Replit rules install correctly to project root as `replit.md`.
