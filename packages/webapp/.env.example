# Registry API URL
NEXT_PUBLIC_REGISTRY_URL=http://localhost:3111

# For production
# NEXT_PUBLIC_REGISTRY_URL=https://registry.prpm.dev

# PostHog Analytics
NEXT_PUBLIC_POSTHOG_KEY=phc_aO5lXLILeylHfb1ynszVwKbQKSzO91UGdXNhN5Q0Snl
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# SSG Data Token (for static site generation)
# Secret token for authenticating SSG data endpoints during build
# Must match the token configured in the registry (SSG_DATA_TOKEN)
# Generate a secure random token: openssl rand -hex 32
# Only needed during build time (not exposed to client)
SSG_DATA_TOKEN=your_secure_random_token_here
